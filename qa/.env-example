# Plugin configuration
PLUGIN_SRC_PATH=/path/to/your/plugin
PLUGIN_DEST_NAME=your-plugin-name

# You can override these values by setting environment variables before running docker-compose
# Example: export PLUGIN_SRC_PATH=/path/to/your/plugin
# Example: export PLUGIN_DEST_NAME=your-plugin-name
